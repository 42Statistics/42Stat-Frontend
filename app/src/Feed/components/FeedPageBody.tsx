import styled from '@emotion/styled';

import { VStack } from '@shared/ui-kit';

export const FeedPageBody = () => {
  return <Layout>{/* <FeedList /> */}</Layout>;
};

const Layout = styled(VStack)`
  width: 100%;
  gap: 4rem;
  min-height: 100vh;
`;
